pip install discord
pip install random
pip install prew
pip install aiohttp
pip install os
wget https://discord.com/api/download?platform=linux&format=deb
sudo apt-get install gdebi -y
gdebi discord-0.0.12.deb
